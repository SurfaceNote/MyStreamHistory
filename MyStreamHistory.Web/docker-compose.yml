services:
  mystreamhistoryweb:
    container_name: mystreamhistorywebcontainer
    build: . 
    image: mystreamhistoryweb
    ports:
     - 80:80
    expose:
     - 80
    volumes:
     - /var/www/certbot:/var/www/certbot
    networks:
      - internalnet

networks:
 internalnet:
  driver: bridge